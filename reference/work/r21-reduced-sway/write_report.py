"""Write the user guide from completed evidence for the final source review."""
from pathlib import Path
import json

ROOT = Path(__file__).resolve().parents[2]
WORK = Path(__file__).resolve().parent
PUBLIC = ROOT/'outputs/Microduck_R13_8_减摆步态与PCB复核'
def read(p): return json.loads((WORK/p).read_text())
def link(label,p): return f'[{label}](<{p}>)'

def main():
    reference = read('geometry/reference_4steps_v3_review/reference_review.json'); assert reference['review_passed']
    artifacts = read('delivery_artifacts.json')['artifacts']
    d = read('dynamics/final_delivery_index.json')
    m = read('dynamics/full_v3/nominal/sway_comparison.json')
    b = m['actual_baseline']['metrics']; c = m['actual_candidate']['metrics']
    cmp = m['amplitude_comparison']
    table = []
    for title,key,unit in [('身体侧倾总摆幅','trunk_roll_deg','°'),
                           ('上头壳固定点左右跨度','head_marker_world_Y_mm','mm'),
                           ('机身根位置左右跨度','root_world_Y_mm','mm'),
                           ('身体左右转向总摆幅','trunk_yaw_deg','°'),
                           ('机身根位置上下跨度','root_world_Z_mm','mm')]:
        bv=b[key]['peak_to_peak']; cv=c[key]['peak_to_peak']; change=100*(cv/bv-1)
        table.append(f'| {title} | {bv:.3f} {unit} | {cv:.3f} {unit} | {change:+.2f}% |')
    cases=[]
    titles={'nominal':'名义工况','half_timestep':'积分步长减半','friction_0p5':'地面摩擦系数0.5','contact9':'足底9片接触划分'}
    for key,case in d['cases'].items():
        s=case['metrics'];cases.append(f'| {titles[key]} | {s["root_error_deg"]:.3f}° | {s["joint_error_deg"]:.3f}° | {s["actual_loaded_group_slip_mm"]:.3f} mm | {s["net_left_forward_mm"]:.2f} / {s["net_right_forward_mm"]:.2f} mm | 通过；零饱和 |')
    model=next(x for x in artifacts if x['path'].endswith('.blend'))
    videos=[x for x in artifacts if x['path'].endswith('.mp4')]
    views='\n'.join('- '+link(Path(x['destination']).stem,PUBLIC/x['destination']) for x in [model]+videos)
    pcbroot=ROOT/'outputs/PCB_功能_DRC与尺寸复核_2026-09-13'
    text=f'''# R21 / R13.8 减摆步态与PCB复核

基于同一套557件结构、名义质量4.195094kg、RK3576核心板与既定电机能力假设，完整四步实际仿真中的身体侧倾摆幅减少 **{cmp['actual_roll_reduction_fraction']*100:.2f}%**，上头壳固定点左右跨度减少 **{cmp['actual_head_marker_Y_reduction_fraction']*100:.2f}%**。原有四步数值验收标准在四种工况下全部通过。结构尺寸、板形、安装孔、电机及扭矩上限沿用R20冻结基线。

这是慢速数值仿真交付。先前设定的身体40%／头部30%优化目标**均未达到**，记录保留原门槛；未进行实物、正常步速、热、低电量持续扭矩或制造验收。

## 摇摆原因与本轮调整

上一版控制参考主动要求机身向左右倾斜约23–24°，实际机身对参考的最大跟踪误差不到1°。因此主要摆幅来自换重动作本身。上方头壳离机身转动中心较远，同样的角度会产生更大的横向位移。

本轮重新求解双足站姿、左右换重、抬脚、前移与落地。按真实足底支撑多边形的面积质心计算，两脚横向距离由171.367mm降至140.466mm，缩窄18.03%；这来自真实关节正向运动学。准备站姿的脚外展参数由15°调整为约27.974°，屈膝与髋/踝配合也一起调整。全脚实体在初态的最窄横向材料间距约4.634mm，不能把它当作全运动最小三维间隙。

当前每腿没有踝关节侧倾轴，脚底姿态、机身姿态与关节负载存在耦合，减摆仍受这些实际机构约束。本轮保持完整脚底、零件比例和质量模型，头部没有另加反向摆动来隐藏躯干侧倾。

## 实际完整四步对照

所有摆幅取各自完整原始实际轨迹的世界坐标，不使用相机、显示横移或阶段重采样。头部测量点固定为同一个上头壳封闭网格的体积中心，既不是整个头部的质量重心，也不是相机画面的中心。

| 指标 | R20 | R21 | 变化 |
|---|---:|---:|---:|
{chr(10).join(table)}

R21实际侧倾范围为−16.130°至+17.239°；时间加权平均侧倾0.806°，起终侧倾约0°。较小的峰峰值没有依靠持续偏身取得。头部前后跨度约增加3.16mm，机身上下跨度约增加0.89mm，这些变化也保留在原始记录中。

四步实际时间由174.847s增加到195.442s，约增加11.78%。两只脚各迈两步、每步参考前移10mm；名义仿真最终左右脚分别前进20.223mm和20.096mm。它仍属于慢速验证动作，不能据此宣称已达到日常行走速度。

| 工况 | 最大机身跟踪误差 | 最大关节跟踪误差 | 实际受力足最大滑移 | 左／右净前进 | 原四步数值标准 |
|---|---:|---:|---:|---:|---|
{chr(10).join(cases)}

滑移补充检查按实际保存的足底法向力大于2N分组，保留计划抬脚阶段仍有短暂接触的记录，不把“计划卸载”当成实际完全离地。名义工况最低足底点的实际抬脚峰值至少3.276mm。四种工况的轨迹、接触记录和验收明细均随更新包交付。

## 结构与扭矩检查的范围

参考轨迹含8399个样本、582段关节直线路径。检查使用完整557件装配：141个选定姿态做全零件材料检查，全部582段逐段检查105219对跨刚体零件。并行只分配原始请求，段、关节值和时间逐条核对，无抽稀或漏段。

实际名义轨迹检查包含9773个关节范围区间，覆盖781767个积分步，累计1,028,305,287项零件对与区间组合。独立复核核对覆盖记录、轨迹端点、关节变换和原始STEP。与R20相比，没有新增未分类的材料穿透。

原先72对参考窄间隙或未完全定性接口、34对实际名义接触/参考表示仍明确保留。原两处颈部网格微残差按原STEP零公共体积核定；两处同一根线的尾端与参考走线表示交叠分别命名记录。这些结果**不是整机全部间隙或柔性线束已经合格**。原六处腿/电池碰撞目标和新左髋连杆在本轮指定检查中没有复发；实际区间的材料分离证明不等于每个区间都已证明2.2mm间隙。

地面检查使用真实源网格：8399个参考样本和9774个实际样本中，555件非脚垫零件均未穿地。实际最小足架离地2.456mm，鞋壳7.560mm；TPU最大数值压入0.544mm仅是接触模型结果，未作为真实弹性压缩验收。

按501条质量来源独立复算重力与关节力矩，参考单足最大利用率约85.00004%；全部给定负载分配最大约85.00077%。单足压力中心边距约8mm；双足分配采用每脚2mm目标，插值后最小1.999589mm，保留这个数值边界。包含惯性的前馈与实际控制另由上述四种工况验证，电机上限和质量/重心仍是未经实测的工程输入。

四步从明确的新准备站姿初始化，两个周期在关节空间闭合并在世界坐标中连续前移，实际积分中不重置机身。旧准备站姿到新站姿的过渡动作单列为候选；从q=0 HOME自主起步尚未取得同等级完整验证，不能随这四步一并放行。

## 查看与复算

{views}

Blender保留原R20的静态整机、头部安装对照、原实际四步三个场景，并增加R21实际四步、同实际时间对照和同阶段对照。零件仍可分别选择与隐藏。对照相机和比例固定，横向排版位移与物理世界轨迹分开。模型保存后重新打开核验源网格、全部动画键和独立关节变换；视频是有明确倍率的预览，完整采样保留在模型中。

同实际时间视图只比较两版都有数据的共同时间段；同阶段视图显示各自实际秒数。正式幅值始终用完整原始轨迹，不能从任意一段预览估计。

本更新包提供完整可直接打开的Blender模型、新轨迹、控制/前馈生成程序与检查证据。复算脚本所需的旧结构源沿用R13.7“工程快照”，本包“工程更新”合并到同一工作根目录；逐项来源与补充文件见随包索引。这些是仿真控制源，不代表已下载并调通RK3576实机。

## PCB清单、DRC与面积

{link('全部PCB功能、主要芯片、尺寸及原生设计入口',pcbroot/'01_全部PCB功能与主要器件.md')}；{link('DRC与缩板评估',pcbroot/'00_PCB检查与缩板结论.md')}；{link('独立PCB完整检查包',ROOT/'outputs/PCB_功能_DRC与尺寸复核_2026-09-13.zip')}。

共12种自制PCB、13块用板，LEG5左右各一块，总板框面积166.81cm²。KiCad 10.0.6按原工程规则运行ERC、重铺铜DRC、未连线及原理图对应检查，12种均为0；另把原来忽略的规则启用后出现43项警告：38处焊线/测试点装配占位边界、5处载板过孔中心问题。逐对象复核没有发现新增断路，仍需补装配定义和整理走线。

缩板优先考虑65×45mm的5V入口板，再评估65×60mm舵机电源板与32×30mm回生控制板的合并；58×45mm核心板载板已经四层且受模块/连接器限制。现有孔位、线束、散热与承流铜形成实际边界，本轮未生成经重排复验的更小尺寸或制造板。

当前557件装配只定位了7块自制PCB，回生、两温保、IMU、功放和麦克风这6块尚待详细安装；U2D2和相机也未完成独立安装实体。相关8组估算质量约63.856g已经计入运动模型，补准确CAD与实测质量时应替换原预留，避免重复计重。模型内的现有板形与本次原生电气保持一致，PCB复核没有改变本轮减摆比较的质量和安装基线。
'''
    (WORK/'报告.md').write_text(text)
    print('GUIDE_WRITTEN',len(text))

if __name__=='__main__': main()
